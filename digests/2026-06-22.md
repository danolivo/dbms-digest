# DBMS Weekly — 2026-06-22 (week of 2026-06-22–2026-06-28)

## PostgreSQL mailing lists
- **[hackers] [Conflict log history table for Logical Replication — error handling and a parallel-apply race get nailed down](http://www.mail-archive.com/pgsql-hackers@lists.postgresql.org/msg231322.html)** — the PG20-targeted feature that records apply-time replication conflicts into a system-managed table (`conflict_log_destination = log | table | all`, living in a `pg_conflict` schema) had its most intensive review week yet (Jun 22–26), and several open design questions were resolved. Highlights:
    - **ERROR, not WARNING, on a failed conflict-log insert.** Amit Kapila settled the long-running question: a failed insert into the conflict-log table now ERRORs out (attaching the conflict info as error context) instead of being silently downgraded to a WARNING — the WARNING path could spam the log on a persistent failure (e.g. disk full while writing WAL), let users miss conflicts, and required an "ugly" nested try/catch that was judged a future bug source.
    - **A parallel-apply race that could lose the conflict row.** Nisha Moond reproduced and fixed a race where, on an ERROR-level conflict, the leader's `pa_wait_for_xact_finish()` unblocks once `AbortOutOfAnyTransaction()` releases the worker's transaction lock, then raises "lost connection to the parallel apply worker" and SIGTERMs it *mid-insert* — losing the deferred conflict row. Fix adds a `PARALLEL_TRANS_ERROR` state plus an injection-point TAP test (`039_pa_conflict_log_lock_wait.pl`).
    - **json vs jsonb, and stricter option parsing.** The stored tuple uses `json` (chosen to preserve exact column order/formatting and avoid a per-conflict conversion), with the caveat that `json` has no ordering operator so `ORDER BY replica_identity` errors — flagged as needing more thought. Also tightened: drop the conflict-log table before its dependency records, and reject an empty-string `conflict_log_destination` to match every other subscription option.

  *(Nisha Moond, Amit Kapila, Dilip Kumar, vignesh C, shveta malik, Peter Smith · pgsql-hackers)* `[patch posted]`

## CommitFest (open: PG20-1, #59)
- **Balance (Jun 22–28):** not captured this run. The per-CF activity log (`/59/activity/`) was not reachable, and the global activity log (`/activity/`) was served from a stale cache (newest rows dated May 25), so a clean week-over-week flow for Jun 22–28 could not be computed without guessing. The open CommitFest **PG20-1 (#59)** is in its collection phase — open now, closes **2026-06-30**, in-progress Jul 1–31. Queue snapshot (cached view, **not** window-scoped, so `[unverified]` for this week): ~287 entries — ~194 Needs review, ~32 Ready for Committer, ~25 Waiting on Author, 3 committed, ~24 moved to next CF, ~9 withdrawn. The week-over-week baseline from last run (PG20-1) therefore carries forward unchanged; fresh per-CF flow tracking resumes once the activity log is reachable again.

---

_1 item · sources scanned: ~12 (Planet PostgreSQL, PostgreSQL news archive, pgsql-hackers via mail-archive.com [date pages + thread chaining], CommitFest root + global activity log, official list archive, pganalyze blog, Habr [ru] search, HN/Algolia search) · filtered out as marketing/ads: ~4 (SEO "PostgreSQL performance tuning 2026" listicles, vendor tuning guides)._

_Honest scope note for this week: the live PostgreSQL infrastructure (specific mail-archive message pages, the news archive, the CommitFest root) is current to ~Jun 26, but two things blocked a wider scan without fabricating — (1) the general web-search index returned only pre-2025 results for 2026 queries, so it could not **discover** in-window blog posts, community threads, or non-English items; and (2) the index/feed pages that would enumerate the week (mail-archive's newest "by date" page, Planet PostgreSQL, the official monthly archive) were served stale-cached or as empty JS shells. The PostgreSQL news archive showed **no** project releases dated inside the window (newest item: postgres-lsp, Jun 21 — last week's window). Rather than invent links, engagement counts, or article content, the Community pulse, Wider DBMS, Commercial engines, Migration, Research, International, and Conferences sections are omitted this week. The one development that could be confirmed in-window from primary sources — the logical-replication conflict-log-table review on pgsql-hackers — is reported above._
